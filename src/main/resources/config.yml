spring:
  datasource:
    url: jdbc:h2:mem:test
    username:
    password:
  h2:
    console:
      enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        stmp:
          auth: true
          starttls:
            enable: true
  jpa:
    defer-datasource-initialization: true
app:
  jwt:
    secret:
